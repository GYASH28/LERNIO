# Test Credentials

## Lernio AI

**Auth provider:** Firebase Auth (Email/Password + Google).
**Important:** This session did not create or modify any auth users. The Firebase project (`lernio-b4c65`) is owned by the user. No test accounts were seeded.

If a test user is needed for browser testing:
- Use the **Sign Up** flow on the live site to register a test email such as `lernio-tester@example.com` with any password (≥6 chars).
- Or sign in with Google via the "Continue with Google" button.

No backend credentials, API keys, or service-account secrets are stored in this repo. They live only in Vercel's environment variable store and on the user's machine.
